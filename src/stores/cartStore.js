import { defineStore } from "pinia";

export const userCartStore = defineStore('cart', () => {
    const cartList = ref([])

    const addCart = () => {
        const res = 
    }

    return {
        cartList,
        addCart
    }
})